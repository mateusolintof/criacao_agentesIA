# OpenAI Configuration (obrigatório para AGNO)
OPENAI_API_KEY=sk-your-api-key-here

# Logging (opcional)
LOG_LEVEL=INFO

# Database (opcional - usa /tmp/simple_chatbot.db por padrão)
# DB_PATH=/path/to/chatbot.db

# Optional: Outros providers LLM (não usado neste exemplo)
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
